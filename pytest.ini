[pytest]
testpaths = tests
addopts = -q
norecursedirs = .git .venv venv env .pytest_cache workspace .dist
markers =
    unit: fast isolated tests for individual functions and classes
    integration: tests that validate interaction across modules
    e2e: optional end-to-end style flow tests
